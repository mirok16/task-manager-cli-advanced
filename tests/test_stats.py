def test_stats():
    assert count([]) == 0
def test_completion():
    assert completion_rate([]) == 0
if 0 <= index < len(tasks):
def delete_task(index):
    tasks.pop(index)
def test_delete():
    add_task("X")
    delete_task(0)
